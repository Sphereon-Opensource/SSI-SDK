version: 3.0

# Agent
agent:
  $require: '@veramo/core#Agent'
  $args:
    - schemaValidation: false
      plugins:
        - $require: ./packages/qr-code-generator#QrCodeProvider
#          $args:
#            - renderingProps
